<Window x:Class="DesktopAdmin.EditLeaveRequestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edycja wniosku"
        Height="560"
        Width="760"
        MinHeight="520"
        MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="170" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="ID" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="IdTextBox" IsReadOnly="True" Margin="0,0,0,8" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Pracownik" VerticalAlignment="Center" Margin="0,0,0,8" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="UserTextBox" IsReadOnly="True" Margin="0,0,0,8" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Typ urlopu" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox Grid.Row="2" Grid.Column="1" x:Name="LeaveTypeComboBox" Margin="0,0,0,8" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Status" VerticalAlignment="Center" Margin="0,0,0,8" />
        <ComboBox Grid.Row="3" Grid.Column="1" x:Name="StatusComboBox" Margin="0,0,0,8" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Data od" VerticalAlignment="Center" Margin="0,0,0,8" />
        <DatePicker Grid.Row="4" Grid.Column="1" x:Name="StartDatePicker" Margin="0,0,0,8" />

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Data do" VerticalAlignment="Center" Margin="0,0,0,8" />
        <DatePicker Grid.Row="5" Grid.Column="1" x:Name="EndDatePicker" Margin="0,0,0,8" />

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Notatka pracownika" VerticalAlignment="Top" Margin="0,0,0,8" />
        <TextBox Grid.Row="6"
                 Grid.Column="1"
                 x:Name="ReasonTextBox"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,8" />

        <TextBlock Grid.Row="7" Grid.Column="0" Text="Komentarz szefa" VerticalAlignment="Top" Margin="0,0,0,8" />
        <TextBox Grid.Row="7"
                 Grid.Column="1"
                 x:Name="ManagerCommentTextBox"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,8" />

        <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Anuluj" Style="{StaticResource NeutralButtonStyle}" Width="120" Margin="0,0,10,0" Click="CancelButton_Click" />
            <Button x:Name="SaveButton" Content="Zapisz" Style="{StaticResource PrimaryButtonStyle}" Width="120" Click="SaveButton_Click" />
        </StackPanel>
    </Grid>
</Window>
